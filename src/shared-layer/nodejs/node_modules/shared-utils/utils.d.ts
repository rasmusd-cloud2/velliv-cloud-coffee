/**
 * Type declarations for the shared Lambda layer.
 * At runtime, Lambdas import from '/opt/nodejs/node_modules/shared-utils/utils.mjs'
 * via NODE_PATH resolution (bare specifier: `import { formatTimestamp } from 'shared-utils'`).
 */

export function formatTimestamp(date?: Date): string;
export function generateOrderId(): string;
